name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    if: >-
      ${{
        (github.event_name == 'push' && !contains(github.event.head_commit.message, '[no-tests]')) ||
        (github.event_name == 'pull_request' &&
          !contains(github.event.pull_request.title, '[no-tests]') &&
          !contains(github.event.pull_request.body || '', '[no-tests]'))
      }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Install UV and Python
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
          python-version: ${{ matrix.python-version }}

      - name: Create virtual environment
        run: uv venv

      - name: Install dependencies
        run: |
          uv sync --group dev --group full

      - name: Run tests
        run: uv run pytest -v
